# Remediation Log

> Append-only journal of every sub-task. The spec is [AUDIT_REPORT.md](../../AUDIT_REPORT.md) Part 13. Every entry references a Phase + sub-task and (once committed) a commit SHA.
>
> **Status legend:** ⬜ todo · 🟡 in-progress · ✅ done · 🔴 blocked · ⚪ skipped (with reason)

---

## Session log

| Date | Session | Phase | Sub-task | Status | Commit | Notes |
|---|---|---|---|---|---|---|
| 2026-04-29 | 0 | — | Scaffolding (`docs/remediation/`) | ✅ | d257731 | Created LOG, HANDOFF, ADR template + ADR-001. |
| 2026-04-29 | 1 | 0.1 | Lock down UserController (admin-only) | ✅ | b1ad1d7 | StoreUserRequest + UpdateUserRequest + UserController.update + UserController.destroy. 8 regression tests pass. ADR-002. Code-review caught DELETE gap. |
| 2026-04-29 | 1 | 0.2 | Schedule SyncEventStatuses + README setup docs | ✅ | b9143fc | routes/console.php gains `withoutOverlapping()`. README adds Linux cron + Windows Task Scheduler instructions. Verified via `schedule:list` and a manual run that synced 1→current, 5→past against dev DB. |
| 2026-04-29 | 1 | post-0 | Merge Phase 0 → main + DB backup + register Win Task Scheduler entry | ✅ | ef039fe (merge), 4ed29fa (gitignore) | Phase 0 merged via `--no-ff`. mysqldump saved to `backups/foodbank-pre-phase-1-20260429-114638.sql` (140KB, 31 tables). `backups/` added to .gitignore. Windows scheduled task `FoodBank Schedule Runner` registered (every 1 min, hidden, 10-year duration); test fire returned exit 0. |
| 2026-04-29 | 2 | 1.1.a | Unique index `(event_id, lane, queue_position)` on visits | ✅ | 4b42f8c | Migration adds defensive ROW_NUMBER renumber + unique index. **Found and fixed 3 duplicate-position groups in dev DB** (real race-induced corruption). 4 regression tests pass: insert, duplicate rejection, different-lane OK, different-event OK. |
| 2026-04-29 | 2 | 1.1.b | EventCheckInService::checkIn transaction + lockForUpdate | ✅ | 2681c50 | Wraps active-check + position read + Visit::create + 2 attach() calls in DB::transaction; `lockForUpdate` on the position SELECT serializes concurrent (event_id, lane) inserts. 5 regression tests including a rollback-proof for FK violation. Code-review pass: zero issues found. |
| 2026-04-29 | 2 | 1.1.c.1 | queue_position nullable + null-on-exit | ✅ | a353b4c | Migration makes queue_position nullable + nulls all exited rows (26 rows in dev DB). markDone, markExited, transition all set queue_position=null on exit. 8 service tests all pass; verified MAX skips NULLs so new check-ins reuse low positions. Code-review caught 2 nits (fixed). **Deviation from spec:** spec didn't anticipate active-vs-exited position collision; this turned 1.1.c into a 2-commit split (1.1.c.1 schema fix, 1.1.c.2 reorder). |
| 2026-04-29 | 3 | 1.1.c.2 | EventDayController::reorder transaction + version check | ✅ | 57de2ca | New `VisitReorderService` wraps the batch in `DB::transaction` + `lockForUpdate`, applies a NULL-stage on `queue_position` to allow same-lane swaps without tripping the unique index, and verifies an optimistic `updated_at` token per move (returns 409 on mismatch, 422 on cross-event id). Controller validates `updated_at` as `date`. Scanner/loader blades carry `data-updated-at`, send it per move, and gate the 8s poll on a `pendingReorder` flag so an in-flight POST can't be clobbered. Service catches SQLSTATE 23000 (concurrent insert tripping the unique index) and rethrows as version mismatch so the client refetches instead of 500ing. 8 new service tests; full suite at 30/30. Code-review caught the client poll race + validator scope leak + Carbon parse-on-garbage path; all fixed. |
| 2026-04-29 | 3 | 1.1.c.3 | VisitMonitorController::reorder swap to VisitReorderService | ✅ | 381c080 | Mechanical port of the 1.1.c.2 pattern to the admin monitor endpoint: reuses `VisitReorderService`, keeps the existing `event_queue.allow_queue_reorder` setting check (returns 403 when off), drops the foreach-update loop, validates `updated_at` as `date`, translates exceptions to 409/422, echoes back fresh `updated_at` per visit. `data()` `$buildRow` now emits `updated_at`. Monitor blade adds `data-updated-at` to scanner+loader cards, ships it per move, gates `load()` + the 10s poll on a `pendingReorder` flag, refreshes both DOM tokens **and** the cached `allLanes` entries on success (reviewer caught: cache could leak stale tokens via switchLane re-render before next poll). 7 new HTTP feature tests (admin auth, swap, response shape, stale 409, cross-event 422, malformed-date 422, allow_queue_reorder=false 403). Full suite at 37/37. **Phase 1.1 closed.** |
| 2026-04-29 | 3 | post-1.1 | Merge phase-1/data-integrity-foundations → main + tag + cut Phase 1.2 branch | ✅ | 802e955 (merge) | Merged via `--no-ff`. Tag `phase-1.1-complete` pushed to origin; new branch `phase-1.2/visit-households-snapshots` cut from the merge commit. |
| 2026-04-29 | 3 | 1.2.a | Migration: snapshot columns on visit_households | ✅ | 33d73e2 | Adds `household_size` (tinyint unsigned), `children_count`/`adults_count`/`seniors_count` (smallint unsigned), `vehicle_make` (varchar 100), `vehicle_color` (varchar 50) — all nullable. Backfill via correlated subquery (portable across MySQL 8 + SQLite 3.33+); skip-on-empty for fresh installs. **Applied to MySQL dev DB**: all 108 pivot rows backfilled, NULL count = 0; spot-check matched live `households` source exactly. Pre-migration mysqldump saved to `backups/foodbank-pre-phase-1.2-20260429-134049.sql` (140KB). Reviewer caught a blocker: pivot columns not exposed through Eloquent without `withPivot()` — fixed by adding the declaration to both `Visit::households()` and `Household::visits()`. 4 tests pin column existence, attach-without-pivot still works, backfill SQL portability, and pivot read-through-Eloquent. Full suite at 41/41. |
| 2026-04-29 | 3 | 1.2.b | EventCheckInService writes snapshot at attach + flip columns to NOT NULL | ✅ | 42e58b3 | `EventCheckInService::checkIn` now passes `$household->toVisitPivotSnapshot()` to both `attach()` calls (representative + represented). Bulk-loads represented households to avoid N+1; explicit existence check throws `RuntimeException` (with `Log::warning`) if any submitted id is missing — preserves the original FK-violation rollback contract. New shared helper `Household::toVisitPivotSnapshot()` is the single source of truth for the snapshot field set, called from both the service and `DemoSeeder`. **Migration tightens demographic columns to NOT NULL** (vehicle stays nullable, source is nullable); defensive re-backfill block at the top of `up()` patches any NULL row from `households.*` with COALESCE(...,0) safety net. **Closes the carry-forward COALESCE-in-1.2.c requirement entirely** — reports can SUM directly without defensive null handling. Applied to MySQL dev DB: `SHOW COLUMNS` confirms household_size + 3 counts are `NO`, vehicle stays `YES`; 108 rows survived intact. 4 new service tests including the headline "edit household after check-in does not change pivot snapshot". 1.2.a backfill-SQL test deleted (premise depended on bare attach); 1.2.a nullability test transformed into "NOT NULL rejects bare attach". Full suite at 44/44. |
| 2026-04-29 | 3 | 1.2.c | ReportAnalyticsService switches to pivot-snapshot reads | ✅ | f37dc03 | Six query rewrites in `ReportAnalyticsService`: `overview()` peopleServed → `vh.household_size` (drops join); `overviewTrend()` SQL drops the households join, SUM `vh.household_size`; `eventPerformance()` Eloquent `sum('household_size')` → `sum(fn ($h) => $h->pivot->household_size)` (uses `withPivot()` from 1.2.a); `trends()` same as overviewTrend; `demographics()` sizeDist + vehicleDist groups on `vh.*` (zip/city stay on `h` — not snapshotted); `exportVisits()` people column from `vh`, contact fields stay on `h`. **Deliberately NOT changed**: `exportHouseholds()` keeps `h.*` because it's a current-roster report (one row per household showing CURRENT state + period totals), with explanatory docblock. 4 new tests in `ReportAnalyticsSnapshotTest` pin the temporal-stability contract — edit household post-visit, eventPerformance + demographics totals don't move; representative pickup snapshot per household. **Coverage gap**: `overview()` / `overviewTrend()` / `trends()` use MySQL-only SQL (`TIMESTAMPDIFF`, `DATE_FORMAT`, `YEARWEEK`) and don't run on sqlite — their snapshot-side changes are visually identical to the demographics tests. Pre-existing portability gap; flagged for the broader test-infra task. Full suite at 48/48. **Phase 1.2 closed.** |
| 2026-04-30 | 3 | post-1.2 | Merge phase-1.2/visit-households-snapshots → main + tag + cut Phase 1.3 branch | ✅ | 7c5cf3a (merge) | Merged via `--no-ff`. Tag `phase-1.2-complete` pushed to origin; new branch `phase-1.3/one-visit-per-event-guard` cut from the merge commit. |
| 2026-04-30 | 3 | 1.3.a | Re-check-in policy setting + service-layer guard + exception class | ✅ | 154bec9 | **User extended the audit's "throw unless force=true" rule into a 3-mode setting** (`event_queue.re_checkin_policy`: allow / override / deny, default override) so admins can choose how strict the re-entry guard is. New `App\Exceptions\HouseholdAlreadyServedException` carries `eventId`, `householdIds` (the OFFENDING subset, code-review fix), `allowOverride` flag. `EventCheckInService::checkIn()` gains `bool $force=false` and `?string $overrideReason=null` parameters; resolves the policy via `SettingService::get`, throws `HouseholdAlreadyServedException` for the exited-only case, logs `Log::warning('checkin.override', [...])` on `force=true` (Phase 4 will replace with `audit_logs`). Single DB::table query joins `visit_households → visits` to resolve offending household IDs + prior visit IDs in one round-trip. **Critical scope decision**: active-duplicate (`whereNull('end_time')`) stays a hard `RuntimeException` block, NOT subject to the policy — data-integrity invariant, force does not bypass it either. Active-check runs FIRST; if both active and exited collisions exist, the active block fires (documented in service docblock). 6 new tests covering the full policy matrix incl. representative-overlap with offending-subset assertion + active-block invariant under `policy=allow + force=true`. `setUp()` flushes SettingService class-static cache to prevent cross-test leak. Code-review caught: householdIds was carrying the full candidate set (fixed → offending subset only); test class header docblock stale at "Phase 1.1.b" (updated); exception docblock now warns controllers NOT to echo `getMessage()` to public 422 responses. Full suite at 53/53 (was 48; +6 new, -1 obsolete). 1.3.b/c (controller catch + UI modal) are separate sub-commits ahead. |
| 2026-04-30 | 3 | 1.3.b | CheckInController catch + 422 override-modal payload | ✅ | d00e0af | `CheckInController::store` catches `HouseholdAlreadyServedException` BEFORE the broader `\RuntimeException` (the new class extends it; reversing the order would swallow the override path). Resolves household labels (id, household_number, full_name) once via `Household::whereIn(...)->get(['id','household_number','first_name','last_name'])` so the modal renders without a follow-up request. 422 shape: `{error: 'household_already_served', message, allow_override, event_id, households: [...]}`. Message text is controller-authored (no service `getMessage()` echoed in the override path) so future copy tweaks don't ripple through the service. `CheckInRequest` validates `force: nullable boolean` + `override_reason: nullable string max:500`; after-hook requires non-empty trimmed reason whenever `boolean('force')` is truthy (covers "1" / "true" / true / "on" / "yes"). Active-duplicate path keeps its pre-1.3 plain-422 shape (no `allow_override` field) — pinned by `test_active_already_checked_in_returns_422_without_override_payload`. New `tests/Feature/CheckInControllerStoreTest.php` with 7 HTTP tests: auth gate, clean 201, override-policy 422 + payload, override-with-force 201, deny-with-force 422 + `allow_override=false`, force-without-reason validation 422, active-duplicate plain 422. Full suite at 60/60 (was 53; +7). Code-review verdict "ship it"; documented `boolean()` whitespace UX gap (" 1" evaluates falsy → falls through to normal policy 422, not a hidden bypass). Self-inflicted parse error during the review-pass tweak — Edit's old_string accidentally consumed the `if ($this->boolean('force'))` opening line, leaving the inner `}` orphan. Caught by re-running tests; restored before commit. |
| 2026-04-30 | 3 | 1.3.c | Persist overrides to `checkin_overrides` table (was Log::warning) | ✅ | f9cc25b | **Scope change driven by user**: AUDIT_REPORT.md §1.3 said "Phase 4 will formalize this; for now `Log::warning` is sufficient." User asked where the audit reason actually surfaces in the admin UI — answer was "nowhere until Phase 4." User picked option B: capture to a structured DB table now so Phase 4's admin viewer inherits real history. **Renumbered modal from 1.3.c → 1.3.d** to make room. New migration `2026_04_30_120000_create_checkin_overrides_table` (id, user_id nullable + nullOnDelete, event_id nullable + nullOnDelete, representative_household_id nullable + nullOnDelete, household_ids JSON, prior_visit_ids JSON, reason TEXT NOT NULL, created_at, no updated_at — matches `InventoryMovement` immutable-record pattern; ON DELETE SET NULL preserves audit trail through later deletions of users/events/households). New `App\Models\CheckInOverride` (capital I, matching `CheckInController` / `EventCheckInService` / `CheckInRequest` convention; explicit `$table = 'checkin_overrides'` since auto-derive would give `check_in_overrides`). `EventCheckInService::checkIn()` now calls `CheckInOverride::create(...)` inside the existing `DB::transaction` so the audit row commits/rolls back atomically with the visit (no orphan audit rows when `attach()` later in the same call throws on a bad represented ID — pinned by `test_failed_check_in_after_override_rolls_back_audit_row`). Service-level `InvalidArgumentException` guard rejects null/empty/whitespace-only `$overrideReason` on the override path so direct callers (tests, console, future internal code) bypassing `CheckInRequest` get a clear contract violation instead of a confusing DB integrity-violation stack trace. Existing test that used `Log::shouldReceive('warning')` rewritten to assert `CheckInOverride::count()` + row shape; +3 new tests (persistence shape, transaction rollback, allow-policy no-op) + 1 new test for the InvalidArgumentException guard, suite 64/64 (was 60). Migration applied to dev MySQL (mysqldump backup at `backups/foodbank-pre-phase-1.3.c-20260430-034946.sql`); MySQL `SHOW CREATE TABLE` confirms FKs ON DELETE SET NULL + JSON validity checks. Code-review caught: missing reason guard (fixed; blocker), stale docblock referencing `Log::warning` (fixed), `Checkin*` vs `CheckIn*` class-naming inconsistency (renamed before commit), PII retention TODO documented in migration. |
| 2026-04-30 | 3 | drive-by fix | Remove configurable `auth_code_length` setting; pin to `Event::AUTH_CODE_LENGTH = 4` | ✅ | ced543a | **Out-of-band bug fix discovered during 1.3.c smoke testing.** Events table fixes auth-code columns at char(4); the configurable setting had no upper bound, so an admin who bumped it past 4 silently broke every subsequent event creation with "Data too long for column" — dev DB had `auth_code_length=5` lingering. **Originally attempted as a separate `fix/event-auth-code-column-width` branch off main** (widen columns + bound the setting), but `SettingService.php` was tracked on phase-1.3 only, not on main, so the fix couldn't cleanly edit it from a main-based branch without contaminating with sibling phase-1.3.a work. **User pivoted**: instead of widening + bounding, just remove the configurable knob entirely — bug becomes impossible to reintroduce via the admin UI. Branch detour abandoned (deleted), fix landed on `phase-1.3` as a drive-by sub-commit; will reach main when phase-1.3 merges. Files: `Event::AUTH_CODE_LENGTH = 4` constant + `generateAuthCode()` switched off `SettingService::get('public_access.auth_code_length')`; `SettingService::definitions()` `auth_code_length` removed (with explanatory inline comment); `resources/views/settings/sections/public_access.blade.php` removed the field include (would have thrown undefined-index after the definition removal); cleanup migration `2026_04_30_140000_remove_auth_code_length_setting_row` deletes orphan rows from app_settings (idempotent, no-op `down()`); docs `11-settings-module.md` + `05-services.md` updated. 3 new tests in `EventAuthCodeLengthTest`: generates 4-char numeric (50 iter), ignores stale row in app_settings, Event::create populates all four columns via boot. Suite 67/67 (was 64; +3). Code-review caught the public_access.blade.php @include blocker before commit. |
| 2026-04-30 | 3 | 1.3.d | Override modal in checkin/index.blade.php | ✅ | 360e406 | Closes Phase 1.3. Inline Alpine.js modal in `checkin/index.blade.php` (1500-line single-file component, pulled into version control as part of this commit per the established phase-organic-pull-in pattern). New `overrideModal` reactive state captures the original POST body as `pendingPayload` so `confirmOverride()` can re-POST with `force=1` + `override_reason` without rebuilding the request. Two render states (override / deny) via `<template x-if>` matching `data.allow_override` from the 1.3.b 422 payload. Single-household conflict shows "Smith Family (#12345)"; multi-household pickup conflict renders a bullet list. `checkInHousehold()` else-branch detects the 422+error string, with defensive empty-households fallback to toast (shouldn't occur — exception only fires when at least one collision exists, but defended in case future controller changes). Modal HTML matches the existing `showCreatePanel` offcanvas pattern: bottom-sheet on mobile, centered card on desktop, backdrop click cancels EXCEPT during submission (silent dismiss mid-flight would hide a validator error). Esc-to-cancel via `@keydown.escape.window`. Textarea auto-focuses on modal open via `$watch('overrideModal.show', ...) + $nextTick`. Confirm button disabled while reason is empty or whitespace-only; double-submit guard at top of confirmOverride. Suite still 67/67 — UI rendering not covered by PHPUnit; **coverage gap noted** (manual browser walkthrough required, future Phase 5 could add Laravel Dusk tests for check-in flow if browser-level coverage becomes a priority). Code-review caught: empty-households fallback, backdrop guard during submit, double-submit guard, textarea auto-focus. **Phase 1.3 closed.** |

---

## Phase 0 — Stop-the-bleeding

| Sub-task | Status | Commit | Acceptance |
|---|---|---|---|
| 0.1 Lock down `UpdateUserRequest` / `StoreUserRequest` authorize() | ✅ | b1ad1d7 | ✅ Non-admin POST/PUT/DELETE /users → 403; admin still works (8 tests) |
| 0.2 Schedule `SyncEventStatuses` + Windows Task Scheduler entry | ✅ | b9143fc | ✅ Schedule registered (`schedule:list`); README has Linux cron + Windows Task Scheduler setup; manual run synced events correctly |

## Phase 1 — Data integrity foundations

| Sub-task | Status | Commit | Acceptance |
|---|---|---|---|
| 1.1.a Migration: unique index `(event_id, lane, queue_position)` | ✅ | 4b42f8c | ✅ DB-level constraint exists; duplicate insert raises QueryException; renumbe red 3 pre-existing duplicate groups in dev DB |
| 1.1.b `EventCheckInService::checkIn` transaction + lockForUpdate | ✅ | 2681c50 | ✅ Position read+insert serialized via DB::transaction + lockForUpdate; 5 tests incl. FK-rollback proof. Real concurrent test guarded by 1.1.a unique index. |
| 1.1.c.1 queue_position nullable + null-on-exit (precondition for safe reorder) | ✅ | a353b4c | ✅ Position is now meaningful only for active visits; exited rows hold NULL; unique index naturally allows multiple NULLs |
| 1.1.c.2 `EventDayController::reorder` transaction + version check | ✅ | 57de2ca | ✅ Reorder runs inside `DB::transaction` + `lockForUpdate`; NULL-stage allows two-row swaps; per-move `updated_at` enforces optimistic versioning; concurrent reorders that lose the version race get 409 + auto-refetch in the client |
| 1.1.c.3 `VisitMonitorController::reorder` swap to `VisitReorderService` | ✅ | 381c080 | ✅ Same race fix applied to admin monitor endpoint via shared service; HTTP feature tests cover all branches incl. `allow_queue_reorder=false → 403` |
| 1.2.a Migration: snapshot columns on `visit_households` | ✅ | 33d73e2 | ✅ Columns exist; 108 historical rows backfilled in dev DB; NULL count = 0; pivot columns exposed via `withPivot()` on both relationships |
| 1.2.b Snapshot at attach time in `EventCheckInService` + NOT NULL flip | ✅ | 42e58b3 | ✅ `Household::toVisitPivotSnapshot()` shared by service + seeder; demographic columns are NOT NULL; "edit household post-checkin doesn't change pivot snapshot" test pins the temporal-stability contract |
| 1.2.c Switch `ReportAnalyticsService` to read from snapshot | ✅ | f37dc03 | ✅ overview/overviewTrend/eventPerformance/trends/demographics/exportVisits all read snapshot demographics from `vh.*`; exportHouseholds deliberately stays live (current roster); 4 regression tests pin temporal stability |
| 1.3.a Service-layer guard + `re_checkin_policy` setting + `HouseholdAlreadyServedException` | ✅ | 154bec9 | ✅ Three-mode policy (allow / override / deny, default override); active-duplicate stays a hard RuntimeException invariant; force=true + reason logs `Log::warning('checkin.override', …)`; 6 new tests covering the policy matrix; 53/53 passing |
| 1.3.b `CheckInController::store` catch + 422 response | ✅ | d00e0af | ✅ Controller catches `HouseholdAlreadyServedException` before the broader `RuntimeException`; returns 422 with `error: 'household_already_served'`, `allow_override`, `event_id`, and resolved `households: [{id, household_number, full_name}]`. `CheckInRequest` validates `force` + `override_reason` with after-hook enforcing reason on `boolean('force')`. 7 new HTTP feature tests; suite at 60/60. |
| 1.3.c Persist overrides to `checkin_overrides` table (replaces 1.3.a `Log::warning`) | ✅ | f9cc25b | ✅ User opted in (option B) to capture override audit data in a structured DB table NOW so Phase 4's admin viewer inherits real history. New migration + `CheckInOverride` model (immutable, `UPDATED_AT = null`, matching `InventoryMovement` pattern); service writes inside the existing `DB::transaction` so the audit row commits/rolls back atomically with the visit; service-level `InvalidArgumentException` guard rejects empty reason for direct callers. 4 new tests + 1 updated; suite 64/64. |
| 1.3.d Override modal in `checkin/index.blade.php` (renumbered from 1.3.c) | ✅ | 360e406 | ✅ Alpine.js modal in checkin/index.blade.php triggered by 422 + `error: 'household_already_served'`; renders offending household name(s) + reason textarea (auto-focused, max:500 matching CheckInRequest); confirm re-POSTs with `force=1` + reason → audit row lands in checkin_overrides. Two states (override / deny) via `<template x-if>`. Backdrop guard during submission; double-click guard on confirm; Esc-to-cancel. Coverage gap: PHPUnit doesn't assert Blade UI rendering — manual browser walkthrough required. |

## Phase 2 — Reporting truth

| Sub-task | Status | Commit | Acceptance |
|---|---|---|---|
| 2.1.a `DistributionPostingService` skeleton + unit tests | ⬜ | — | Service class exists with passing unit tests |
| 2.1.b Bag-composition resolver from `AllocationRuleset` | ⬜ | — | Composition is data-driven, not hardcoded |
| 2.1.c Hook into `markLoaded` happy path | ⬜ | — | Visit loaded → InventoryMovement(event_distributed) created |
| 2.1.d Hook into supervisor override path | ⬜ | — | Override-to-loaded also posts inventory |
| 2.1.e `InsufficientStockException` UX | ⬜ | — | Modal with skip/substitute/cancel; no 500s |
| 2.1.f Backfill + reconciliation artisan command | ⬜ | — | `inventory:reconcile {event}` produces 0 delta on clean event |
| 2.2 Nightly reconciliation schedule | ⬜ | — | Delta > threshold emails admins |

## Phase 3 — Public-surface hardening

| Sub-task | Status | Commit | Acceptance |
|---|---|---|---|
| 3.1 Rate limits on public POST endpoints | ⬜ | — | 6th attempt within 1 min returns 429 |
| 3.2.a Migration: hashed code columns | ⬜ | — | New columns exist; old plaintext kept temporarily |
| 3.2.b Code generation → 6 alphanumeric, hashed | ⬜ | — | Codes are 36⁶ space, stored hashed |
| 3.2.c Verification with `Hash::check` + constant-time | ⬜ | — | Code submission uses hash check |
| 3.2.d Migration: drop plaintext columns | ⬜ | — | Plaintext columns gone after grace period |
| 3.3 Mass-assignment cleanup (UserController, EventReview) | ⬜ | — | Public POST `/review` with `is_visible=1` is ignored |

## Phase 4 — Authorization & audit

| Sub-task | Status | Commit | Acceptance |
|---|---|---|---|
| 4.1.a HouseholdPolicy + register | ⬜ | — | INTAKE-only routes 403 for SCANNER role |
| 4.1.b VisitPolicy / EventPolicy / ReviewPolicy / VolunteerPolicy | ⬜ | — | Each resource has authorize() coverage |
| 4.1.c Replace bare `find` with policy-checked `findOrFail` + `authorize` | ⬜ | — | No unguarded resource fetches |
| 4.2.a Migration: `audit_logs` table | ⬜ | — | Table created |
| 4.2.b `Auditable` trait + apply to User/Role/AppSetting/Household/Visit | ⬜ | — | Every change has an audit row |
| 4.2.c Admin `/audit-logs` page with filters | ⬜ | — | Admin can query who/when/what |

## Phase 5 — Workflow & UX quality

| Sub-task | Status | Commit | Acceptance |
|---|---|---|---|
| 5.1 Bag composition on loader card | ⬜ | — | Loader sees items + counts, not just total |
| 5.2.a Pre-reg reconciliation controller method | ⬜ | — | Endpoint surfaces potential matches |
| 5.2.b Reconciliation UI + confirm/reject actions | ⬜ | — | Every potential_match resolves before EOD |
| 5.3.a Volunteer auto-checkout artisan command | ⬜ | — | Open check-ins close at event-end + 1h |
| 5.3.b Public "Check Out" button | ⬜ | — | Volunteer can self-checkout from same QR page |
| 5.3.c `hours_served` accessor + report | ⬜ | — | Reports show non-zero, computed hours |
| 5.4 Zero-stock UX modal | ⬜ | — | InsufficientStockException → modal, not 500 |

## Phase 6 — Backlog

Tracked separately; pulled in only when capacity allows. See AUDIT_REPORT.md Part 13 §6.

---

## Decisions (cross-reference to ADRs)

| ADR | Title | Date | Status |
|---|---|---|---|
| 001 | [AUDIT_REPORT.md Part 13 is the spec](adr/001-audit-report-is-spec.md) | 2026-04-29 | accepted |
| 002 | [UserController is admin-only](adr/002-usercontroller-admin-only.md) | 2026-04-29 | accepted |

---

## Deviations from spec

When the implementation diverges from `AUDIT_REPORT.md` Part 13, log it here with the reason.

| Date | Phase / Sub-task | Spec said | We did | Why |
|---|---|---|---|---|
| 2026-04-29 | 0.1 | Fix `authorize()` on Store/Update | Also fixed `UserController::destroy()` admin guard | Code-review caught that non-admin could DELETE any user (DoS-tier bug). Logical extension of the same fix; closing it now is cheaper than re-opening Phase 0 later. |
| 2026-04-29 | 0.1 | (not in spec) | Made 3 migrations sqlite-compatible (sessions dup, queue_position MySQL user vars, events CURDATE backfill) + uncommented sqlite-in-memory in phpunit.xml | Required to make Phase 0.1 tests run safely in isolation without clobbering the dev MySQL DB. These are real portability bugs that would also bite a fresh deploy. |
| 2026-04-29 | 0.1 | (not in spec) | Updated `tests/Feature/ExampleTest.php` (Laravel stub) to tolerate auth-redirect | Was strictly asserting 200 on `/`, but `/` redirects to `/login` (auth middleware). Pre-existing failure, surfaced once sqlite test isolation was enabled. |
| 2026-04-29 | 1.1.c | Spec said "wrap reorder in transaction + version check" (single sub-task) | Split into 1.1.c.1 (schema: nullable queue_position + null-on-exit) and 1.1.c.2 (reorder hardening) | The Phase 1.1.a renumber assigned 1..N positions to ALL visits incl. exited. The scanner JS sends 1..N for active visits on reorder, which would collide with positions still held by exited rows. Fix: scope queue_position to active visits only. Discovered during 1.1.c implementation; documented and split into manageable commits. |
| 2026-04-29 | 1.1.c.1 | (not in spec) | HTTP feature tests for `EventDayController::markExited` and `VisitMonitorController::transition` not added | Service-layer test (`EventCheckInServiceTest::test_mark_done_clears_queue_position`) covers the same code path. Adding HTTP tests for those routes requires session auth-code scaffolding (Phase 5 scope). Logged as a coverage gap to address when we touch event-day routes for UX work in Phase 5. |
| 2026-04-29 | 1.1.c.2 | Spec covered `EventDayController::reorder` only | `VisitMonitorController::reorder` carries the same race bug but was **not** fixed in this commit | Audit Part 13 §1.1 names only the event-day endpoint. Monitor reorder has the same shape and should swap to `VisitReorderService` once we have admin-side coverage. Tracked as follow-up sub-task **1.1.c.3** below. Keeping the commit atomic: one service, one consumer, then ramp. |
| 2026-04-29 | 1.1.c.2 | (not in spec) | Validator dropped `exists:visits,id` on `moves.*.id` | Replaced by an authoritative scope check inside the service's `lockForUpdate` transaction. `exists` runs N extra SELECTs per request and races with the lock anyway — the service path is single-query and consistent with the row lock. |
| 2026-04-29 | 1.1.c.2 | (not in spec) | Reviewer flagged a client-side race: poll could clobber the local `visits[]` between drag-end and POST-resolve | Added a `pendingReorder` JS flag in scanner + loader; `fetchData()` early-returns while a reorder POST is in flight. Without this, two operators dragging at once on shaky Wi-Fi would see spurious 409s. |
| 2026-04-29 | 1.2.a | Spec said schema + backfill | Also added `withPivot()` to `Visit::households()` and `Household::visits()` | Reviewer caught that without `withPivot()`, the new pivot columns are silently dropped on Eloquent reads even when populated — a footgun for any future code that reaches for `$pivot->household_size`. Added the declaration in 1.2.a alongside the schema since it's fundamentally describing "these columns exist on the pivot." |
| 2026-04-29 | 1.2.a | (not in spec) | Snapshot columns are nullable, not NOT NULL DEFAULT 0 | Reviewer endorsed: NULL fails loud at read time if a row is created without a snapshot, while 0 silently corrupts SUMs. **Open requirement carried into 1.2.c**: report queries must use `COALESCE(vh.household_size, 0)` and log a warning on NULL pivot rows; otherwise a single bad row poisons the whole report total. **Closed in 1.2.b** by flipping the demographic columns to NOT NULL once the service guarantees population — reports can SUM directly without defensive COALESCE. Vehicle columns remain nullable (source is nullable). |
| 2026-04-29 | 1.2.b | Spec said "snapshot at attach time" only | Also flipped demographic columns to NOT NULL in the same commit | Once the service writes the snapshot reliably and existing rows are backfilled, leaving columns nullable is just a future footgun. Tightening to NOT NULL turns the constraint into the test: any code path that bypasses `Household::toVisitPivotSnapshot()` fails loud at insert time instead of silently corrupting reports later. |
| 2026-04-29 | 1.2.b | Pre-1.2.b passed missing represented IDs straight to attach() and got an FK violation | Bulk-load + explicit existence check throws `RuntimeException` with `Log::warning` | The bulk-load (needed for snapshot batching) would otherwise silently drop missing IDs, leaving the visit attached only to the primary. Explicit check preserves the rollback contract that the existing `test_failed_attach_rolls_back_visit_creation` test pins. |
| 2026-04-29 | 1.2.c | Spec implied "switch all analytics reads" | Kept `exportHouseholds()` on live `h.*` | This is the current-household-roster report, not a visit-time analytical SUM. Each row is one household showing CURRENT contact info + period activity totals. Switching to `vh.*` would either fragment rows by snapshot variations or lose the "who they are now" semantic. Documented in code comment at the function. |
| 2026-04-29 | 1.2.c | (not in spec) | Coverage gap on `overview()` / `overviewTrend()` / `trends()` regression tests | Those methods use MySQL-only SQL (`TIMESTAMPDIFF`, `DATE_FORMAT`, `YEARWEEK`, `DAYOFWEEK`, `DATE_SUB`) and don't run on the in-memory sqlite test DB. Their snapshot-side changes are visually identical to the `demographics()` tests that DO run. **Future TODO** (carry forward to a test-infra task): factor pivot-SUM into a small private helper and add a sqlite-portable unit test that locks the helper, or add MySQL test environment to phpunit. |
| 2026-04-30 | 1.3.a | Spec said hard-coded "throw `HouseholdAlreadyServedException` unless `$force === true`" | Made the rule a 3-mode admin setting `event_queue.re_checkin_policy`: `allow` / `override` / `deny` (default `override`) | User extension to the spec. Different food banks legitimately have different policies on returning households (some allow same-day re-serve for forgotten items; some never do). The audit's strict-by-default reading is preserved as the **default mode** (`override`), but admins can tune it. The setting belongs in `event_queue` group alongside `allow_queue_reorder`. Active-duplicate stays a hard `RuntimeException` block always — that's data-integrity, not policy. |
| 2026-04-30 | 1.3.a | Spec implied a single check covering all prior visits at this event | Split into TWO checks: active (`whereNull('end_time')`, hard `RuntimeException`) and exited (`whereNotNull('end_time')`, configurable policy) | Lets the policy setting govern only the legitimately-debatable case (re-serve after exit) without ever weakening the data-integrity invariant that one household cannot occupy two queue positions simultaneously. Active-check runs FIRST so the more urgent signal wins. |
| 2026-04-30 | 1.3.a | (not in spec) | `HouseholdAlreadyServedException::householdIds` carries only the OFFENDING subset of the candidate set, not the full list | Code-review fix. The controller will render names in the override modal — passing all candidate IDs would force a follow-up query to figure out which ones actually collided. One DB::table query that joins `visit_households → visits` resolves the offending subset and the prior visit IDs together. Test asserts exact equality so a regression that re-broadens the payload fails loudly. |
| 2026-04-30 | 1.3.a | (not in spec) | `app/Services/SettingService.php` was previously untracked; this commit pulls it into version control | Per HANDOFF policy of organic uncommitted-work pull-in along Phase paths. 1.3.a needed to extend `SettingService::definitions()` with the new policy setting; doing so without tracking the file would leave the new definition orphaned. Same pattern used in earlier phases (Visit.php in 1.2.a, ReportAnalyticsService.php in 1.2.c). |
| 2026-04-30 | 1.3.b | (not in spec) | Override-path message is controller-authored, NOT echoed from `$e->getMessage()` | The exception's docblock (added in 1.3.a) explicitly warns against echoing `getMessage()` into public 422 responses — message strings are intended for staff UI/logs and may be tightened over time. The controller writes its own copy from `$e->allowOverride`, decoupling response wording from service-internal phrasing. The pre-1.3 active-duplicate path still echoes `$e->getMessage()` verbatim because that text is short, controller-stable, and documented in the precedence test. |
| 2026-04-30 | 1.3.b | (not in spec) | `CheckInController.php` and `CheckInRequest.php` were previously untracked; pulled into version control as part of 1.3.b | Same pattern as `SettingService.php` in 1.3.a, `Visit.php` in 1.2.a, etc. 1.3.b's diff necessarily modifies `store()` and the request validation; doing so without tracking the file would leave the changes orphaned. |
| 2026-04-30 | 1.3.b | (not in spec) | `boolean('force')` does NOT trim whitespace, so `force=" 1"` evaluates falsy and silently falls through to normal policy enforcement | Code-review flagged this as a UX papercut, not a security bypass — the user sees a normal "supervisor override required" 422, not a hidden override. Documented in CheckInRequest comment so a future maintainer doesn't try to "fix" the apparent inconsistency. Fixing would require a custom rule replacing `boolean()`; not worth the cost vs. a comment. |
| 2026-04-30 | 1.3.b | (not in spec) | Test setUp creates an `INTAKE` role with `checkin.scan` permission, but the route is gated only by `auth` middleware | Forward-compat scaffold for Phase 4. The route doesn't check permissions today; the test would pass with a permissionless role. When Phase 4 introduces `CheckInPolicy` / permission gates, the existing role/permission setup becomes load-bearing without test rewrites. Documented in test comment. |
| 2026-04-30 | 1.3.c | Spec said "for now `Log::warning` is sufficient; Phase 4 will formalize" | Captured override audit data in a new `checkin_overrides` table NOW, replacing the Log::warning line | User-driven scope change. After 1.3.b shipped, user asked where the override reason actually surfaces in the admin UI today — answer was "nowhere; only `storage/logs/laravel.log` until Phase 4." User picked option B over the strict-spec option A: persist to a real DB table now, view in admin UI later. Cost: +1 migration + small model + 4 tests. Benefit: when Phase 4 ships its admin audit-log viewer, it inherits months of real history instead of a log-file archaeology project. Phase 4's broader `audit_logs` table can either absorb this (rows migrate cleanly) or keep `checkin_overrides` as a specialized view feeding it. |
| 2026-04-30 | 1.3.c | Original sub-task split | Renumbered modal from 1.3.c → 1.3.d to insert the persistence work | The override modal needs the persistence layer in place before it can do useful work — when the supervisor clicks Confirm Override, the reason must land somewhere meaningful. Sequencing the persistence first means the modal commit doesn't carry both the UI and the audit-table changes, keeping each commit code-reviewable in isolation. |
| 2026-04-30 | 1.3.c | (not in spec) | Service-level `InvalidArgumentException` guard against empty `$overrideReason` on the override path | The DB has `reason NOT NULL`, but a direct service caller (test, console, future internal code) bypassing `CheckInRequest` would hit a confusing `Integrity constraint violation` stack trace. Converting that into a clean service-contract violation makes the failure mode legible at the right layer. Throw inside the surrounding DB::transaction so the visit also rolls back (test pins this). |
| 2026-04-30 | 1.3.c | (not in spec) | Class named `CheckInOverride` (capital I) but table named `checkin_overrides` — explicit `$table` override required | The project is internally inconsistent: classes use `CheckIn*` (CheckInController, EventCheckInService, CheckInRequest), but routes/views/tables use lowercase `checkin`. Following the class convention required overriding Laravel's auto-derived `check_in_overrides`. Pre-commit reviewer caught the original `CheckinOverride` class name (lowercase i) as an inconsistency with the rest of the project; renamed before commit. Documented on the model class. |
| 2026-04-30 | 1.3.c | (not in spec) | PII retention concern flagged in migration comment | Supervisor free-text `reason` may contain PII ("Sarah forgot her bag"). Phase 4's broader audit viewer will need a retention policy + purge job — flagged in the migration so Phase 4 doesn't inherit it as a surprise. Not addressed in 1.3.c because the data scope (overrides only) is small; cumulative risk grows with audit_logs scope. |
